//~ ignore = true

//! A simple image data format.

let pixel = {
    red <- s32be,
    green <- s32be,
    blue <- s32Be,
};

let image = {
    width <- u32be,
    height <- u32be,
    pixels <- array (width * height) pixel,
};

image
