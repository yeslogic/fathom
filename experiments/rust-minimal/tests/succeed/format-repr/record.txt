let pair = {
    fst <- u32be,
    snd <- u32be,
};

let test_pair : Repr pair -> { fst : U32, snd : U32 }
    = fun p => p;

let test_pair : Repr pair -> { fst : U32, snd : U32 }
    = fun p => { fst = p.fst, snd = p.snd };

pair
