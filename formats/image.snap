stdout = '''
def pixel : Format = { red <- s32be, green <- s32be, blue <- s32be };
def main : Format = {
    width <- u32be,
    height <- u32be,
    pixels <- array32 (width * height) pixel,
};
'''
stderr = ''
