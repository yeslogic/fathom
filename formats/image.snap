stdout = '''
let pixel : _ = { red <- s32be, green <- s32be, blue <- s32be };
let main : _ = {
    width <- u32be,
    height <- u32be,
    pixels <- array32 (u32_mul width height) pixel,
};
main : Format
'''
stderr = ''
