// PTS rules

TypeType = Int -> Int : Type;   // (Type, Type, Type)
TypeKind = Int -> Type;         // (Type, Kind, Kind)
KindType = Type -> Int;         // (Kind, Type, Kind)
KindKind = Type -> Type;        // (Kind, Kind, Kind)

// Common types

ListType = Type -> Type;
ArrayType = Int -> Type -> Type;
ListFormat = Format -> Format;
ArrayFormat = Int -> Format -> Format;
