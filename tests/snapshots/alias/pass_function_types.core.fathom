TypeType = global Int -> global Int : Type;

TypeKind = global Int -> Type;

KindType = Type -> global Int;

KindKind = Type -> Type;

ListType = Type -> Type;

ArrayType = global Int -> Type -> Type;

ListFormat = Format -> Format;

ArrayFormat = global Int -> Format -> Format;
