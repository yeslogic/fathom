stdout = '''
let pair = { fst <- u32be, snd <- u32be };
let test_pair = fun (p : { fst : U32, snd : U32 }) => p;
let test_pair = fun (p : { fst : U32, snd : U32 }) => {
    fst = p.fst,
    snd = p.snd,
};
pair : Format
'''
stderr = ''
