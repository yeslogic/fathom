let f : U32 -> U32 -> U32
= fun x y =>
let p = (x, y);
match p {
    (0, a) => a + (1 : U32),
    (b, 0) => b + (2 : U32),
    (1, c) => c + (3 : U32),
    (d, 1) => d + (4 : U32),
    (_, _) =>     (5 : U32),
};
{}
